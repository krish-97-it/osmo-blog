{% extends 'base.twig' %}

{% block content %}
  <main class="posts">
    <div class="posts__container">
      {% for post in posts %}
        {% include ['tease-post.twig'] %}
      {% endfor %}
    </div>
    <div class="pagination">
      {{ wp_pagenavi }}
    </div>
  </main>
{% endblock %}
